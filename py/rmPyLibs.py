# rmPyLibs.py

	# Ejemplos de try

	    def existeUsr(self, user):
	        try:
	            pwd.getpwnam(user)
	            return 1
	        except:
	            return 0

    # Salidas por pantalla.
    	print "An HTTP error %d occurred:\n%s" % (e.resp.status, e.content)
        
        print "The upload failed with an unexpected response: %s" % response

    # Condiciones e IFs
    	x = 10 if (y == 9) else 20
    	